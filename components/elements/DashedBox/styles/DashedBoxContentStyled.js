'use strict';

import styled from 'styled-components';
import { defaultTheme } from '../../../../styles/theme';

const propTypes = {};
const defaultProps = {};

/** STYLES */
export const DashedBoxContentStyled = styled.div`  
  padding: 40px;
  padding-left: 70px;
`;

DashedBoxContentStyled.displayName = 'DashedBoxContentStyled';
DashedBoxContentStyled.propTypes = propTypes;
DashedBoxContentStyled.defaultProps = defaultProps;
