'use strict';

import styled from 'styled-components';

const propTypes = {};
const defaultProps = {};

/** STYLES */
export const RouteWrapperStyled = styled.main`
  width: 100%;
`;

RouteWrapperStyled.displayName = 'RouteWrapperStyled';
RouteWrapperStyled.propTypes = propTypes;
RouteWrapperStyled.defaultProps = defaultProps;
