'use strict';

import styled from 'styled-components';
import { defaultTheme } from '../../../../../styles/theme';

const propTypes = {};
const defaultProps = {};

/** STYLES */
export const ProjectItemStyled = styled.div`
  position: relative;
`;

ProjectItemStyled.displayName = 'ProjectItemStyled';
ProjectItemStyled.propTypes = propTypes;
ProjectItemStyled.defaultProps = defaultProps;
