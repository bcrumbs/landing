import styled from 'styled-components';
import { defaultTheme } from '../../../../../styles/theme';

export const ProjectButtonRowStyled = styled.div`
  margin-top: 20px;
  margin-left: -${defaultTheme.baseModule[1] * 2.25}px;
`;
