'use strict';

import styled from 'styled-components';
import { defaultTheme } from '../../../../../styles/theme';

const propTypes = {};
const defaultProps = {};

/** STYLES */
export const ProjectItemContentStyled = styled.div`
  position: relative;
  z-index: 1;
`;

ProjectItemContentStyled.displayName = 'ProjectItemContentStyled';
ProjectItemContentStyled.propTypes = propTypes;
ProjectItemContentStyled.defaultProps = defaultProps;
